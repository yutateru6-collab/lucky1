# lucky 開発ルール

- 核は「小さな迷いに50/50の偶然を借りる」。仕様は `docs/CONCEPT.md` を参照。
- 結果に従う義務を作らない。見送る・反対を選ぶ・未決定を正当に扱う。
- 確率は50/50固定。ユーザーの好みや過去データで秘密に偏らせない。
- 抽選結果と本人の選択は別データ。本人の意思表示を「実行済み」と断定しない。
- 金銭報酬・ストリーク・未達ペナルティ・緊急判断への誘導を加えない。
- 個人のメモは `textContent` で表示。外部送信・位置情報取得は明示的な新規要件なしに追加しない。
- 入力のキーワード検出は不完全な補助。安全性やAI機能を誇大に説明しない。
- `npm run check` と `npm run build` を実行する。可能なら `tests/ui_offline_test.py` と実際の公開URLで画面確認。
- PWA配信ファイルを変更したら `public/sw.js` のキャッシュ名を更新する。
- 公開を報告するときは実際のデプロイ成功とURLを確認する。GitHub反映と本番公開を混同しない。
